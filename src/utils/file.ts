/**
 * @description: 文件url 拼接
 * @return {*}
 * @param {string} url
 */
export const getFileUrl = (url: string | undefined) => {
  // const config = useRuntimeConfig()
  // return `${config.public.fileUrl}${url}`
  return url
}
