# 图片处理逻辑整理说明

## 整理前的问题

1. **重复代码**：`optimizeImagesForPuppeteer` 和 `optimizeImagesForPrint` 两个函数逻辑高度重复
2. **冗余示例代码**：存在大量示例URL的注释代码
3. **参数分散**：图片优化参数在不同函数中重复定义，难以维护
4. **逻辑混乱**：图片处理逻辑分散在多个地方，不够清晰

## 整理后的架构

### 1. 统一配置表
```typescript
const IMAGE_OPTIMIZATION_CONFIG = {
  cover: {      // 封面图片
    normal: { width: 900, quality: 100, format: 'webp' },
    print: { width: 600, quality: 40, format: 'jpg' },
    puppeteer: { width: 800, quality: 100, format: 'jpg' }
  },
  content: {    // 内容图片
    normal: { width: 600, quality: 85, format: 'webp' },
    print: { width: 200, quality: 15, format: 'jpg' },
    puppeteer: { width: 250, quality: 70, format: 'jpg' }
  },
  avatar: {     // 头像图片
    normal: { width: 120, quality: 85, format: 'webp' },
    print: { width: 50, quality: 25, format: 'jpg' },
    puppeteer: { width: 100, quality: 50, format: 'jpg' }
  },
  other: {      // 其他图片
    normal: { width: 400, quality: 80, format: 'webp' },
    print: { width: 150, quality: 10, format: 'jpg' },
    puppeteer: { width: 250, quality: 50, format: 'jpg' }
  }
}
```

### 2. 核心函数

#### `optimizeImageUrl(url, options)`
- 基础图片优化函数
- 支持阿里云OSS处理参数
- 自动检测Puppeteer环境
- 统一处理格式转换和质量压缩

#### `getImageType(img)`
- 根据CSS类名识别图片类型
- 返回 'cover' | 'content' | 'avatar' | 'other'

#### `optimizeImagesByEnvironment(environment)`
- 统一的图片优化处理函数
- 支持三种环境：'normal' | 'print' | 'puppeteer'
- 自动应用对应的优化配置

#### `restoreImageUrls()`
- 恢复图片原始URL
- 清理临时数据

### 3. 环境检测和处理

#### `isPuppeteerEnvironment()`
- 检测是否为Puppeteer环境
- 支持多种检测方式

#### `optimizeForPuppeteer()`
- Puppeteer环境优化
- 移除装饰性元素
- 添加优化样式

#### `handleBeforePrint()` / `handleAfterPrint()`
- 打印前后的处理逻辑
- 容器高度调整
- 图片优化和恢复

### 4. 调试功能

#### `testImageOptimization(environment)`
- 手动测试图片优化效果
- 3秒后自动恢复原始图片
- 支持控制台查看优化过程

#### `debugContainerHeight()`
- 调试容器高度信息
- 输出详细的尺寸数据

## 图片优化策略

### 普通浏览访问 (normal)
- 使用WebP格式提升加载速度
- 适中的质量和尺寸
- 保证用户体验

### 浏览器打印 (print)
- 使用JPG格式保证兼容性
- 激进压缩减小PDF体积
- 平衡质量和文件大小

### Puppeteer生成 (puppeteer)
- 专门优化的参数配置
- 保持封面图片质量
- 其他图片适度压缩

## 图片类型分类

1. **封面图片** (footer-image)
   - 最重要的视觉元素
   - 保持相对较高的质量
   
2. **内容图片** (park-image, carrier-image, floor-image)
   - 可以接受较大压缩
   - 优先考虑文件大小
   
3. **头像图片** (broker-avatar)
   - 尺寸小但需要清晰
   - 中等压缩策略
   
4. **其他图片**
   - 默认激进压缩
   - 最小化文件影响

## 使用方法

### 页面加载时
```typescript
// 自动检测Puppeteer环境并优化
optimizeForPuppeteer()
```

### 打印时
```typescript
// 优化打印
printReport() // 会调用 optimizeImagesByEnvironment('print')

// 原始打印
printReportWithoutOptimization() // 不进行图片优化
```

### 手动测试
```typescript
// 测试打印环境优化效果
testImageOptimization('print')
```

## 优势

1. **维护性好**：所有配置集中管理，修改方便
2. **扩展性强**：新增图片类型或环境只需要扩展配置表
3. **逻辑清晰**：职责分离，每个函数功能单一
4. **调试友好**：提供多种调试工具和测试方法
5. **性能优化**：根据不同环境采用最佳策略

## 注意事项

1. 图片优化依赖阿里云OSS的实时处理能力
2. 测试功能仅在开发环境可见
3. Puppeteer环境会自动隐藏调试信息
4. 图片恢复功能依赖data-original-src属性
